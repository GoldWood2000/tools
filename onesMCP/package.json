{
  "name": "orbit-ones-workbench",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "vite build",
    "prestart": "npm run build",
    "start": "node server.js",
    "check": "npm run build && node --check server.js && node --check scripts/dev.js && node server.js --self-check"
  },
  "dependencies": {
    "@radix-ui/react-select": "^2.3.7",
    "motion": "^12.23.24",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  }
}
